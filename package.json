{
  "name": "will-wallet",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "yarn workspace @will-wallet/frontend dev",
    "deploy": "yarn workspace @will-wallet/backend deploy",
    "compile": "yarn workspace @will-wallet/backend compile"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/backend",
      "**/backend/**"
    ]
  },
  "devDependencies": {
    "@types/react": "^18.2.0"
  }
}
